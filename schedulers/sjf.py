def run_sjf(processes, preemptive = False):
    # Processes = list of Process objects
    # returns: 
    # timeline = list of (pid, start, end) tuples
    # results = list of Process objects with .start .finish .waiting .turnaround .filled in
    return timeline, results
